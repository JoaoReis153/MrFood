name: gateway
namespace: mrfood

image: kong:3.9

resources:
  requests:
    cpu: "50m"
    memory: "256Mi"
  limits:
    cpu: "200m"
    memory: "512Mi"

autoscaling:
  minReplicas: 2
  maxReplicas: 4

service:
  type: LoadBalancer