name: notification
image: europe-southwest1-docker.pkg.dev/mrfood-490623/mrfood-repo/notification:ff22f5b
port: 50058

resources:
  requests:
    cpu: "10m"
    memory: "16Mi"
  limits:
    cpu: "50m"
    memory: "64Mi"

autoscaling:
  minReplicas: 1
  maxReplicas: 2

service:
  type: ClusterIP

env:
  secrets:
    SMTP_PASSWORD: wpbp rgwe hplj smzy
  config:
    NOTIFICATION_SERVER_PORT: "50058"
    APP_LOG_LEVEL: debug
    SMTP_FROM: mrfood.api@gmail.com
    SMTP_USER: mrfood.api@gmail.com
    NOTIFICATION_REDIS_HOST: 10.89.138.75
    NOTIFICATION_REDIS_PORT: "6379"
    SMTP_HOST: smtp.gmail.com
    SMTP_PORT: "587"
