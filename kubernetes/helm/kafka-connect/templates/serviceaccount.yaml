apiVersion: v1
kind: ServiceAccount
metadata:
  name: cdc
  namespace: {{ .Values.namespace }}
  annotations:
    iam.gke.io/gcp-service-account: cdc-sa@{{ .Values.gcpProjectId }}.iam.gserviceaccount.com
