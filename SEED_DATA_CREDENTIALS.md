# Seed Data Credentials

This file documents the default credentials used for seed/test data generated by the CSV data pipeline.

## Default User Password

All users created by `make generate-csv` use the following password:

```
Username: (as generated from source data)
Password: mrfood123
```

### Password Hash (bcrypt)

```
$2a$10$h7oCCYsXlom0bwjwYE7md.82mYEHrXlD9oq3tAJSKsqOTzkhfiR3q
```

### How to Use

When testing or developing:

1. Generate seed data:

   ```bash
   make generate-csv
   ```

2. Load data into databases:

   ```bash
   make load-all
   ```

3. Log in to services using any generated username with password **`mrfood123`**

###Example Test Users

Generated usernames follow the pattern: `user_<index>`, `<name>_<index>`, etc.

Example:

- Username: `user_1`, Password: `mrfood123`
- Username: `alice_smith_2`, Password: `mrfood123`

### Security Note

⚠️ **DO NOT USE** these credentials in production. This default password is only for development and testing. Always generate strong, unique passwords for production environments.
