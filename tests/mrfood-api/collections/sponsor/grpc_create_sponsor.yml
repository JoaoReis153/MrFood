info:
  name: CreateSponsor
  type: grpc
  seq: 1

grpc:
  url: localhost:50054
  method: /proto.SponsorService/Sponsor
  methodType: unary
  protoFilePath: ../../../../services/sponsor/internal/api/grpc/proto/protofile.proto
  message: |-
    {
      "id": 1,
      "tier": 4
    }
  auth:
    type: bearer
    token: "{{token}}"
