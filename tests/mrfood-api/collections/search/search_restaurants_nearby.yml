info:
  name: Search restaurants nearby
  type: http
  seq: 13
  tags:
    - Search

http:
  method: GET
  url: "{{baseUrl}}/search"
  params:
    - name: page
      value: "1"
      type: query
    - name: limit
      value: "10"
      type: query
    - name: latitude
      value: "38.716"
      type: query
    - name: longitude
      value: "-9.139"
      type: query
    - name: radius_meters
      value: "5000"
      type: query
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("search nearby returns 200", () => {
          expect(res.status).to.equal(200);
        });

        if (res.status === 200) {
          test("response has data array", () => {
            expect(res.body).to.have.property("data");
            expect(res.body.data).to.be.an("array");
          });

          test("response has pagination", () => {
            expect(res.body).to.have.property("pagination");
          });
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
