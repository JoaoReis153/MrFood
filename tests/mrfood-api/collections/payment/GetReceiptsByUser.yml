info:
  name: GetReceiptsByUser
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/payment"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
