info:
  name: Get receipts by user
  type: http
  seq: 4
  tags:
    - Payment

http:
  method: GET
  url: "{{baseUrl}}/payment"
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("get receipts by user returns 200", () => {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
