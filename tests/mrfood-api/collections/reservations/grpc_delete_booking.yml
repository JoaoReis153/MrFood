info:
  name: DeleteBooking
  type: grpc
  seq: 5

grpc:
  url: localhost:50053
  method: /proto.BookingService/DeleteBooking
  methodType: unary
  protoFilePath: ../../../../services/booking/internal/api/grpc/proto/protofile.proto
  message: |-
    {
      "booking_id": 1
    }
  auth:
    type: bearer
    token: "{{token}}"
