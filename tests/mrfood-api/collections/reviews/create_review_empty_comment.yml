info:
  name: Create review with empty comment
  type: http
  seq: 8
  tags:
    - Reviews

http:
  method: POST
  url: "{{baseUrl}}/reviews/{{restaurantId}}"
  body:
    type: json
    data: |-
      {
        "rating": 4,
        "comment": "   "
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("blank comment returns 400", () => {
          expect(res.status).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
