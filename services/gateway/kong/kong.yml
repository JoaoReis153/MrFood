_format_version: "3.0"

services:
  - name: auth
    host: auth
    port: 50051
    protocol: grpc

    routes:
      - name: auth
        protocols:
        - http
        - https
        paths:
          - /auth

    plugins:
      - name: grpc-gateway
        config:
          proto: /etc/kong/proto/auth.proto

  # mister example for later
  # - name: protected-api
  #   host: my-api
  #   port: 8080

  #   routes:
  #     - name: protected-api
  #       paths:
  #         - /api

  #   plugins:
  #     - name: jwt
  #       config:
  #         claims_to_verify:
  #           - exp

consumers:
  - username: my-client
    jwt_secrets:
      - key: my-client-issuer
        secret: this-is-a-temporary-secret-that-we-should-probably-change
        algorithm: HS256