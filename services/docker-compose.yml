version: "3.8"

services:
  kong:
    image: '${GW_IMAGE:-kong/kong-gateway:3.13.0.2}'
    container_name: kong
    restart: on-failure
    networks:
      - kong-net

    environment:
      KONG_DATABASE: "off"                         # Disable database
      KONG_DECLARATIVE_CONFIG: /etc/kong/kong.yml  # Path to config file


      KONG_PROXY_LISTEN: 0.0.0.0:8000, 0.0.0.0:8443 ssl
      KONG_ADMIN_LISTEN: 0.0.0.0:8001              # Admin API (optional in DB-less)

    volumes:
      - ./gateway/kong/kong.yml:/etc/kong/kong.yml:Z
      - ./gateway/kong/proto:/etc/kong/proto:Z

    ports:
      - "8000:8000"   # Proxy HTTP
      - "8443:8443"   # Proxy HTTPS
      - "8001:8001"   # Admin API (read-only in DB-less)

  auth:
    build: ./auth
    volumes:
      - ./auth:/app
    ports:
      - "50051:50051"
    environment:
      DB_HOST: auth_db
      DB_PORT: 5432
      DB_NAME: mrfood_auth
      DB_USER: auth
      DB_PASS: authsecret
      APP_LOG_LEVEL: debug
      APP_JWT_SECRET: Z3Vlc3R3aG9zdHdvcmxkZm9yZXZlcmNoYW5nZXN3b3JkMTIzNDU2
    depends_on:
      auth_db:
        condition: service_healthy
    networks:
      - app-network
      - kong-net

  auth_db:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: mrfood_auth
      POSTGRES_USER: auth
      POSTGRES_PASSWORD: authsecret
    volumes:
      - auth_db_data:/var/lib/postgresql/data
      - ./auth/db_setup.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U auth -d mrfood_auth"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    command: postgres -c ssl=off -c log_statement=all
    networks:
      - app-network
  
  booking:
    build: ./booking
    ports:
      - "50060:50060"
    environment:
      DB_HOST: booking_db
      DB_PORT: 5432
      DB_NAME: mrfood_booking
      DB_USER: booking
      DB_PASS: bookingsecret
    depends_on:
      booking_db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - app-network
      - kong-net

  booking_db:
    image: postgres:16
    ports:
      - "5440:5432"
    environment:
      POSTGRES_DB: mrfood_booking
      POSTGRES_USER: booking
      POSTGRES_PASSWORD: bookingsecret
    volumes:
      - booking_db_data:/var/lib/postgresql/data
      - ./booking/db_setup.sql:/docker-entrypoint-initdb.d/init.sql:Z
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U booking -d mrfood_booking"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - app-network

networks:
  kong-net:      # Custom bridge network for isolated Kong
    driver: bridge
  app-network:
    driver: bridge

volumes:
  auth_db_data:
  booking_db_data: